{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppvsdbg",
            "name": "Launch (Windows)",
            "request": "launch",
            "program": "${workspaceFolder}\\bin\\KitsunEngine.exe",
            "cwd": "${workspaceFolder}\\bin\\",
            "preLaunchTask": "Build"
        }
    ]
}